# Implementação — Autorização RBAC

Estrutura inicial da API demonstrativa do sistema de delivery.

As regras de autorização no servidor e os testes de segurança serão adicionados
nas próximas etapas.

## Executar a API

```bash
pip install -r requirements.txt
uvicorn app.main:app --reload
```

